# Stellarium Sky Placeholder

This folder is the clean restart boundary for the future `/sky-engine` runtime.

- It intentionally does not import from the deleted legacy sky runtime tree.
- It intentionally does not mount any custom renderer, owner trial module, harness module, or painter-port code.
- The next task should mount a working runtime from the repository's `/study` Stellarium source.