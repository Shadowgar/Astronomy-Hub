import React from 'react'

// Disabled starfield for performance. Returns a lightweight no-op so layout/markup
// that expects <Starfield /> remains valid. Re-enable by restoring the original
// canvas animation implementation.
export default function Starfield() {
  return null
}
