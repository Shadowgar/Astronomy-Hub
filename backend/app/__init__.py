"""Backend app package for Phase 2.5C (Step 1).

This package is intentionally minimal — it only exists so the new
FastAPI entrypoint can be a proper Python package while remaining
additive and non-breaking to the existing runtime.
"""

__all__ = []
