# Evidence Index

This index links command runs, raw outputs, screenshots, and JSON artifacts used to pass/fail module gates.

## Entry Template

| Evidence ID | Module | Gate | Command / Probe | Artifact Path | Result | Notes |
|---|---|---|---|---|---|---|

## Current Entries

| Evidence ID | Module | Gate | Command / Probe | Artifact Path | Result | Notes |
|---|---|---|---|---|---|---|
| EV-0001 | module0-foundation-lock | G4 | Five-case deterministic replay run (v1) | `.cursor-artifacts/parity-compare/phase0-deterministic-five-case.json` | FAIL | Hub and Stellarium stability checks not fully passing. |
| EV-0002 | module0-foundation-lock | G4 | Five-case deterministic replay run (v3) | `.cursor-artifacts/parity-compare/phase0-deterministic-five-case-v3.json` | FAIL | Fingerprint acquisition incomplete for some runtime cases. |
| EV-0003 | module0-foundation-lock | G6 | `npm run test -- test_sky_observer_service.test.js test_sky_clock_service.test.js` | Terminal command output | PASS | Observer/time-scale and deterministic clock regression tests passing. |
| EV-0004 | module0-foundation-lock | G6 | `npm run typecheck` | Terminal command output | PASS | Type safety intact after foundation updates. |
| EV-0005 | module0-foundation-lock | G0 | Exhaustive file inventory seeded at fail-closed baseline | `docs/runtime/port/module-inventory.md` | PASS | All 146 source C/H files enumerated with initial status. |
| EV-0006 | module0-foundation-lock | G3 | TT-based observer hash alignment update | `frontend/src/features/sky-engine/engine/sky/runtime/observerUpdateHash.ts` | PASS | Update hash now keyed on TT Julian date instead of raw ISO string. |
| EV-0007 | module0-foundation-lock | G6 | `npm run test -- test_sky_observer_service.test.js` | Terminal command output | PASS | Observer service regression suite green after TT-hash change. |
| EV-0008 | module0-foundation-lock | G0 | Module0 file rows in `module-inventory.md` set to `PORTED`/`BLOCKED` + AH mapping; `deltat.c` **PORTED** | `docs/runtime/port/module-inventory.md` (commit) | PASS | Cleared **`UNMAPPED`** for all `module0-foundation-lock` file rows at the time; **global** zero-**`UNMAPPED`** inventory is recorded under **EV-0019**. |
| EV-0009 | module0-foundation-lock | G6 | `npm run type-check` && `npx vitest run tests/test_erfa_nutation.test.js tests/test_time_scales.test.js` | Terminal command output | PASS | Typecheck + foundation time/ERFA tests green after inventory closure workflow. |
| EV-0010 | module1-hips-kernel | G0 | ~~Module1 inventory pre-close~~ **SUPERSEDED** | — | INVALID | Reverted: execution order is module 0 to **COMPLETE** before module 1 inventory closure (`module-gates.md` note). |
| EV-0011 | module0-foundation-lock | G4 | `npx vitest run tests/test_module0_deterministic_replay.test.js` | `frontend/tests/__snapshots__/test_module0_deterministic_replay.test.js.snap` + `module0ParityFingerprint.ts` | PASS | Hub five-case deterministic fingerprint + snapshot; pair with **EV-0017** for BLK-000 tier-1; native second runtime → **BLK-002**. |
| EV-0012 | module0-foundation-lock | G1 | Review / adopt `module0-source-contract.md` as partial source-contract lock | `docs/runtime/port/module0-source-contract.md` (commit) | PASS | Frozen Hub file list + Stellarium §1 scope for observer spine; §5 states remaining G1 gap for other module 0 files. |
| EV-0013 | module0-foundation-lock | G1 | Author `module0-eraEpv00-port-plan.md` (line span, generator strategy, test + integration checklist) | `docs/runtime/port/module0-eraEpv00-port-plan.md` (commit) | PASS | Staged plan for `eraEpv00`; implementation tracked via future evidence rows. |
| EV-0014 | module0-foundation-lock | G6 | `npx vitest run tests/test_erfa_epv00.test.js tests/test_module0_deterministic_replay.test.js` | `frontend/scripts/generate_erfa_epv00_tables.mjs`, `erfaEpv00.ts`, `erfaEpv00Tables.generated.ts`, `observerDerivedGeometry.ts`, snapshot update | PASS | `eraEpv00` ported from study `erfa.c`; PyERFA goldens; `earthPv`/`sunPv` wired; Module 0 fingerprint snapshot refreshed. |
| EV-0015 | module0-foundation-lock | G1 | Author `module0-eraApco-port-plan.md` + implement **`eraApcs`** with regression tests | `docs/runtime/port/module0-eraApco-port-plan.md`, `erfaApcs.ts`, `erfaConstants.ts` (`ERFA_DAU`…), `tests/test_erfa_apcs.test.js` | PASS | First `eraApco` subgraph ported; full `eraApco` + `astrom` wiring still open (plan §4). |
| EV-0016 | module0-foundation-lock | G6 | `npx vitest run tests/test_erfa_apco.test.js` | `erfaApco.ts`, `test_erfa_apco.test.js` (SOFA `test_ufunc.test_apco` vector) | PASS | **`eraApco`** + helpers; **`deriveObserverGeometry`** wires **`astrom`** (UTC `theta`, `refa`/`refb`=0); apparent-place / `eraRefco` still TODO. |
| EV-0017 | module0-foundation-lock | G4 | `npx vitest run tests/test_module0_replay_astrom_golden_contract.test.js` | `tests/fixtures/module0_replay_astrom_goldens.json`, `npm run parity:dump-module0-astrom-goldens`, `.github/workflows/module0-parity.yml` | PASS | Tier-1 BLK-000 exit: five-case **`astrom`** + **`ri2h[0][0]`** contract vs committed Hub goldens; native Stellarium / PyERFA second runtime → **BLK-002**. |
| EV-0018 | module0-foundation-lock | G5 | `cd study/module0-parity && python -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt && python verify_apco_vs_hub.py` | `study/module0-parity/verify_apco_vs_hub.py`, `study/module0-parity/requirements.txt`, fixture `apcoInputs` (v2 schema) | PASS | BLK-002 exit: PyERFA **`erfa.apco`** matches Hub **`astrom`** slice for five replay cases; CI runs this after `pip install` in `module0-parity.yml`. |
| EV-0019 | module0-foundation-lock | G0–G7 | `npm run typecheck && npm run build && npx vitest run tests/test_sky_observer_service.test.js tests/test_sky_clock_service.test.js tests/test_time_scales.test.js tests/test_erfa_nutation.test.js tests/test_erfa_epv00.test.js tests/test_erfa_apcs.test.js tests/test_erfa_apco.test.js tests/test_module0_deterministic_replay.test.js tests/test_module0_replay_astrom_golden_contract.test.js` (from `frontend/`) | `docs/runtime/port/module-gates.md` (module 0 row **COMPLETE**), `module-inventory.md` (G0 seed, zero **`UNMAPPED`**), `module0-source-contract.md` §6 | PASS | Module 0 gate closure bundle: typecheck + production build + foundation Vitest set. Full `npm test` remains for **module8-global-final-gate** per `module-gates.md`. |
| EV-0020 | module1-hips-kernel | G0–G7 kickoff | `npm run typecheck && npm run build && npm run test:module1` (from `frontend/`; **`test:module1`** = `vitest run` on tile/eph/runtime + **`test_module1_deterministic_replay`**) | `module1-source-contract.md`, `module-gates.md` (module 1 row), `frontend/package.json` (`test:module1`) | PASS | Historical: first filled module 1 gate row; full closure superseded by **EV-0034**; **`test_close_fov_star_counts.test.js`** excluded (long-run / asset-heavy). |
| EV-0021 | module1-hips-kernel | G6 | GitHub Actions workflow for module1 regression bundle (`typecheck` + `build` + `test:module1`) | `.github/workflows/module1-hips.yml` | PASS | Automates module1 G6 checks on PR/push paths touching tile adapters/contracts/tests; keeps module1 gate signal aligned with EV-0020 command bundle. |
| EV-0022 | module1-hips-kernel | G3 (partial) | `npm run test:module1` (includes `test_file_backed_tile_repository_bounds.test.js`) | `fileTileRepository.ts` (RA wrap bounds helpers), `tests/test_file_backed_tile_repository_bounds.test.js`, `frontend/package.json`, `.github/workflows/module1-hips.yml` | PASS | Runtime-path seam hardening: RA wrap intervals (`raMinDeg > raMaxDeg`) now handled for healpix pixel preselection and star clipping; test bundle expanded from 13 to 15 assertions. |
| EV-0023 | module1-hips-kernel | G3 (partial) | `npm run test:module1` (includes `test_file_backed_tile_repository_gaia_flow.test.js`) | `tests/test_file_backed_tile_repository_gaia_flow.test.js`, `fileTileRepository.ts`, `.github/workflows/module1-hips.yml` | PASS | Runtime-path proof: narrow-FOV Gaia activation follows query → mirror/properties fetch → HiPS tile path fetch → `decodeEphTile` merge with in-bounds filtering into emitted tile payload. |
| EV-0024 | module1-hips-kernel | G4 | `npx vitest run tests/test_module1_deterministic_replay.test.js` | `frontend/src/features/sky-engine/engine/sky/runtime/module1ParityFingerprint.ts`, `frontend/tests/module1ReplayFixtures.ts`, `frontend/tests/__snapshots__/test_module1_deterministic_replay.test.js.snap` | PASS | Hub **`SkyTileRepositoryLoadResult`** fingerprint (sorted tiles/stars, fixed decimals); snapshot drift guard; included in **`npm run test:module1`**. |
| EV-0025 | module1-hips-kernel | G2 (partial) | `npm run test:module1` (includes `test_eph_codec.test.js`) | `ephCodec.ts` (`decodeEphTileNuniq`, `encodeEphTileNuniq`), `tests/test_eph_codec.test.js` | PASS | **`nuniq` ↔ order/pix** matches Stellarium **`eph-file.c`** `eph_read_tile_header`; tile decode path uses shared helper. |
| EV-0026 | module1-hips-kernel | G2/G3 (partial) | `npm run test:module1` (includes `test_eph_codec.test.js`, **`test_healpix.test.js`**) | `ephCodec.ts` (`shuffleEphTableBytes`), `healpix.ts`, `tests/test_healpix.test.js` | PASS | **`eph_shuffle_bytes`**-aligned shuffle + inverse; HEALPix nest **pix → center → pix** round-trip samples (orders 0–3). |
| EV-0027 | module1-hips-kernel | G2 (partial) | `npm run test:module1` (`test_eph_codec.test.js`) | `ephCodec.ts` (`convertEphFloat`, exported **`EPH_UNIT_*`**) | PASS | **`eph_convert_f`** parity: deg↔rad, arcsec→deg, per-year bit 8, legacy **`EPH_ARCSEC_`** normalization. |
| EV-0028 | module1-hips-kernel | G3 (partial) | `npm run test:module1` (`test_tile_selection.test.js`) | `tileSelection.ts`, `tests/test_tile_selection.test.js` | PASS | **`selectVisibleTileIds`** regression: wide FOV → depth 1; telescopic FOV → depth 3; **`maxTileLevel`** cap; constants documented as G2/G5 debt in **`module1-source-contract.md` §3**. |
| EV-0029 | module1-hips-kernel | G5 (partial) | `npm run test:module1` (`test_hips_render_order.test.js`) | `hipsRenderOrder.ts`, `tests/test_hips_render_order.test.js`, `sky/index.ts` exports | PASS | Stellarium **`hips_get_render_order`** formula + **`hips_render`** clamp (**`fmin(..., 9)`**). |
| EV-0030 | module1-hips-kernel | G5 (partial) | `npm run test:module1` (`test_hips_render_order.test.js`, `test_file_backed_tile_repository_gaia_flow.test.js`) | `hipsRenderOrder.ts` (`resolveGaiaHealpixOrder`), `contracts/tiles.ts` (`hipsViewport`), `fileTileRepository.ts` | PASS | Gaia Eph fetch order = **`max(quadtree level order, screen order)`** when **`hipsViewport`** set; cache key includes viewport; integration test **`Norder5`**. |
| EV-0031 | module1-hips-kernel | G5 (partial) | `npm run typecheck` + **`npm run test:module1`** (includes `test_scene_query_state.test.js`) | `SkyEngineScene.tsx`, `SkyProjectionService.ts` (`getViewportHeight`), `buildSkyEngineQuery`, `sceneQueryState.ts` (`buildRuntimeTileQuerySignature`) | PASS | Live scene passes **`hipsViewport`** into tile queries; tile signature includes viewport so Gaia order tracks resize/projection. |
| EV-0032 | module1-hips-kernel | G2 / G5 | `npm run test:module1` (`test_hips_render_order.test.js`, `test_tile_selection.test.js`) | `hipsRenderOrder.ts` (`normalizeProjectionMat11ForHips`, `buildSyntheticHipsViewportForTileSelection`), `projectionMath.ts` (`buildCanonicalSkyProjectionViewForFov`), `tileSelection.ts`, `SkyEngineScene.tsx` | PASS | **`projectionMat11`** stored normalized for `hips_get_render_order`; Hipparcos **`selectVisibleTileIds`** depth = **`max(mag tier, clampHipsRenderOrder(hipsGetUnclamped(...), 0, maxLevel))`** with live or synthetic viewport. |
| EV-0033 | module1-hips-kernel | G3 | `npm run test:module1` (`test_tile_selection.test.js`) | `buildSkyEngineQuery`, `tileSelection.ts`, `loadSkyRuntimeTiles` in `SkyEngineScene.tsx` | PASS | **`visibleTileIds`** change with **`hipsViewport`** at fixed observer/FOV; reload path passes **`query.hipsViewport`** into **`selectVisibleTileIds`**. |
| EV-0034 | module1-hips-kernel | G6 / G7 | `npm run typecheck && npm run build && npm run test:module1` (from `frontend/`) | `docs/runtime/port/module-gates.md` (module 1 **COMPLETE**), `docs/runtime/port/evidence-index.md` (through **EV-0034**) | PASS | Module 1 gate closure: typecheck + production build + module1 Vitest bundle; evidence bundle linked. |
| EV-0035 | module2-stars-full | Execution order | Review **`module-gates.md`** + **`module-inventory.md`** (`Planned Module == module2-stars-full`) | `docs/runtime/port/module-gates.md` (module 2 active-target note) | PASS | **`module1-hips-kernel`** **COMPLETE** (**EV-0034**); **`module2-stars-full`** promoted as next target; G1 contract follows in **EV-0036**. |
| EV-0036 | module2-stars-full | G0 / G1 | Inventory rows (`hip.c`, `hip.h`, `stars.c`, `bv_to_rgb.c`) + author **`module2-source-contract.md`** | `docs/runtime/port/module2-source-contract.md`, `docs/runtime/port/module-gates.md` (module 2 **`G0`/`G1`**) | PASS | G1 mapping for §1–§2 Hub targets. |
| EV-0037 | module2-stars-full | G1 / G5 (reference) | Review **`stellarium-web-engine-src.md`** + **`blockers.md`** (**`BLK-003`** **RESOLVED**) | `docs/runtime/port/stellarium-web-engine-src.md` (commit **`63fb3279e85782158a6df63649f1c8a1837b7846`**) | PASS | Pinned **Stellarium/stellarium-web-engine** revision + `src/` paths for module 2 inventory rows. |
| EV-0038 | module2-stars-full | G2 (partial) | `npm run test:module2` (`vitest run tests/test_module2_bv_to_rgb.test.js`) | `bvToRgb.ts`, `starRenderer.ts` (`resolveStarColorHex`), `test_module2_bv_to_rgb.test.js`, `package.json` (`test:module2`) | PASS | Stellarium **`bv_to_rgb`** 128×3 **`COLORS`** table + index math ported; hex wrapper regression. |
| EV-0039 | module2-stars-full | G2 / G3 (partial) | `npm run test:module2` (includes `test_module2_stars_nuniq.test.js`) | `starsNuniq.ts`, `ephCodec.ts` (`decodeEphTileNuniq`), `test_module2_stars_nuniq.test.js`, `.github/workflows/module2-stars.yml` | PASS | **`stars.c` `nuniq_to_pix`** documented alias to **`decodeEphTileNuniq`** (same as **`eph-file.c`**). |
| EV-0040 | module2-stars-full | G2 (partial) | `npm run test:module2` (includes `test_module2_stars_render_limit.test.js`) | `stellariumPainterLimits.ts` (`resolveStarsRenderLimitMagnitude`), `StarsModule.ts`, `test_module2_stars_render_limit.test.js`, `.github/workflows/module2-stars.yml` | PASS | **`stars.c` `render_visitor`** `limit_mag = fmin(stars_limit_mag, hard_limit_mag)` aligned with Hub `min(exposure limitingMagnitude, …)`; unit-tested. |
| EV-0041 | module2-stars-full | G2 (partial) | `npm run test:module2` (includes `test_module2_hip_get_pix.test.js`) | `hipGetPix.ts`, vendored `hipPixOrder2.generated.ts`, `test_module2_hip_get_pix.test.js`, `.github/workflows/module2-stars.yml` | PASS | Stellarium **`hip.c` `hip_get_pix`** + **`hip.inl` `PIX_ORDER_2`** ported; table is vendored in-app with no external source dependency. |
| EV-0042 | module2-stars-full | G3 (partial) | `npm run test:module2` + `npm run test:module1` (tile merge path) | `hipGetPix.ts` (`parseHipIdFromRuntimeStar`, `runtimeStarMatchesHipHealpixLookup`), `fileTileRepository.ts` (`mergeSurveyTiles` Hipparcos filter), `test_module2_hip_get_pix.test.js` | PASS | Hipparcos survey merge drops rows where **`hip_get_pix(hip,2)`** ≠ **`healpixAngToPix(2, ra, dec)`** (or unknown HIP). |
| EV-0043 | module2-stars-full | G4 (partial) | `npm run test:module2` (includes `test_module2_deterministic_replay.test.js`) | `module2ParityFingerprint.ts` (`computeModule2PortFingerprint`), snapshot **`test_module2_deterministic_replay.test.js.snap`** | PASS | Deterministic fingerprint over BV + nuniq + **`resolveStarsRenderLimitMagnitude`** + **`hip_get_pix`** samples (no I/O). |
| EV-0044 | module2-stars-full | G3 (partial) | `npm run test:module2` (includes `test_module2_stars_lookup.test.js`) | `starsLookup.ts` (`findRuntimeStarByHipInTiles`), `hipGetPix.ts`, `test_module2_stars_lookup.test.js` | PASS | Hub `obj_get_by_hip`-style helper: reject invalid HIP mapping (`hip_get_pix` order 0/1), skip Gaia rows, return first non-Gaia HIP match from loaded tiles. |
| EV-0045 | module2-stars-full | G3 (partial) | `npm run test:module2` + `npm run test:module1` + `npm run build` after wiring lookup into scene runtime | `SkyEngineScene.tsx` (`buildEngineStarSceneObjects` uses `findRuntimeStarByHipInTiles` + `parseHipIdFromRuntimeStar`) | PASS | Live scene star objects now carry HIP-lookup truth note status from loaded runtime tiles (non-Gaia path), exercising `obj_get_by_hip` seam on active engine path. |
| EV-0046 | module2-stars-full | G3 (partial) | `npm run test:module2` + `npm run test:module1` + `npm run build` | `starsLookup.ts` (`buildHipDetailRoute`, `resolveHipDetailRouteForRuntimeStar`), `SkyEngineScene.tsx` (`detailRoute` for HIP stars), `test_module2_stars_lookup.test.js` | PASS | Runtime star objects now emit stable HIP detail routes (`hip/<id>`) while preserving `obj_get_by_hip` lookup semantics. |
| EV-0047 | module2-stars-full | G3 (partial) | `npm run test:module2` + `npm run test:module1` + `npm run build` | `useSkyEngineSelection.ts` (`resolveSelectedObjectWithDetailRoute` + id-resync effect), `test_sky_engine_selection_detail_route.test.js` | PASS | Selection continuity now survives runtime star-id churn by matching on stable HIP `detailRoute` (`hip/<id>`) and resyncing `selectedObjectId` to the active object id. |
| EV-0048 | module2-stars-full | G3 (partial) | `npm run typecheck && npm run test:module2 && npm run test:module1 && npm run build` | historical `SkyEnginePage.tsx` / `styles.css` pass | PASS | Historical UI/staging pass; later parity updates supersede this state. |
| EV-0049 | module2-stars-full | G3 (partial) | `npm run typecheck && npm run test:module2 && npm run test:module1 && npm run build` | `SkyEnginePage.tsx` (control-strip structure), `styles.css` (`--stellarium` hud layout classes) | PASS | HUD control layout reshaped into Stellarium-like grouping: center action strip + right-side time controls while preserving existing runtime bindings. |
| EV-0050 | module2-stars-full | G3 (partial) | `npm run typecheck && npm run test:module2 && npm run test:module1 && npm run build` | `SkyEnginePage.tsx` (icon-first top tools + compact control labels), `styles.css` (`icon-chip`, tighter shell geometry) | PASS | Port pass shifts controls from Hub text-heavy chips toward Stellarium-style compact icon chrome and denser shell geometry while preserving engine bindings. |
| EV-0051 | module2-stars-full | G3 (partial) | `npm run typecheck && npm run test:module2 && npm run test:module1 && npm run build` | historical `SkyEnginePage.tsx` / `styles.css` dock geometry pass | PASS | Historical left-dock experiment; later reverted during direct parity correction. |
| EV-0052 | module2-stars-full | G3 (partial) | `npm run typecheck && npm run test:module2 && npm run test:module1 && npm run build` | historical `SkyEnginePage.tsx` / `styles.css` dock geometry pass | PASS | Historical right-dock experiment; later reverted during direct parity correction. |
| EV-0053 | module2-stars-full | G3 (partial) | `npm run typecheck && npm run test:module2 && npm run test:module1 && npm run build` | `frontend/public/stellarium-web/*.svg` (from `apps/simple-html/static/imgs/symbols/`), `stellariumWebUiAssets.ts`, `SkyEnginePage.tsx`, `stellarium-web-engine-src.md` (UI asset note), `.github/workflows/module2-stars.yml` | PASS | Vendored simple-html toolbar SVGs are wired in-app; runtime UI assets are self-contained. |
| EV-0054 | module2-stars-full | G4 (partial) | `npm run test:module2` (`test_module2_deterministic_replay.test.js`); `npm run typecheck && npm run build` | `module2ParityFingerprint.ts` (extended `computeModule2PortFingerprint`: `coreGetPointForMagnitude` + view-tier LOD mirror), `test_module2_deterministic_replay.test.js.snap` | PASS | G4 fingerprint now also covers Stellarium point pipeline samples and FOV tier/label-cap policy aligned with `resolveViewTier` (StarsModule path). |
| EV-0055 | module2-stars-full | G3 (partial) | `npm run typecheck && npm run test:module2` | `SkyEnginePage.tsx`, `styles.css` | PASS | Direct parity correction pass: removed synthetic left/right docks, kept centered bottom control cluster, added Equatorial J2000 grid control, and restored top search icon structure. |
| EV-0056 | module2-stars-full | G7 (partial) | `rg` audit + doc reconciliation pass for `docs/runtime/port/*.md`; `npm run typecheck && npm run test:module2` | `README.md`, `module-gates.md`, `module2-source-contract.md`, `evidence-index.md`, `stellarium-web-engine-src.md`, `module-inventory.md`, `blockers.md`, `module0-source-contract.md`, `module1-source-contract.md`, `module0-eraApco-port-plan.md`, `module0-eraEpv00-port-plan.md`, `AUDIT-2026-04-20.md` | PASS | Full runtime-port doc audit for Codex handoff: removed stale local-source assumptions, marked superseded UI evidence, and codified exact-parity + self-contained asset/tooling rules. |
| EV-0057 | module2-stars-full | G3 (stability partial) | `npm run typecheck && npm run test:module2` | `SkyEngineScene.tsx` (`RUNTIME_MODEL_SYNC_CADENCE_MS` 200→500; wide-FOV query floor `6.5`) | PASS | Recovery pass for current runtime blockers: reduced model-sync churn to lower UI lag and prevented wide-FOV star query collapse that produced near-empty star renders. |
| EV-0058 | module2-stars-full | G3 (stability partial) | `npm run typecheck && npm run test:module2` | `runtimeFrame.ts` (`resolveProjectedStarCapForFov`, render limiting-magnitude floor `6.5`) | PASS | Added projected star count caps by FOV tier to reduce thin-instance upload pressure and aligned render-side limiting-magnitude floor so stars remain visible while scene-stability recovery is in progress. |
| EV-0059 | module2-stars-full | G3 (interaction partial) | `npm run typecheck && npm run test:module2` | `types.ts` (`SkyEngineAidVisibility` expanded), `aidVisibilityPersistence.ts`, `SkyEnginePage.tsx`, `DsoRuntimeModule.ts`, `AtmosphereModule.ts`, `LandscapeModule.ts`, `SkyEngineScene.tsx` | PASS | Wired bottom toolbar toggles to runtime state: Deep Sky / Atmosphere / Landscape now drive render modules via `aidVisibility`; Night Mode now uses shared engine visibility state instead of UI-local toggle only. |
| EV-0060 | module2-stars-full | G3 (stability partial) | `npm run typecheck && npm run test:module2` | `SkyEngineScene.tsx` (`RUNTIME_MODEL_SYNC_CADENCE_MS` 1000), `runtimeFrame.ts` (`resolveProjectedStarCapForFov`: 1200/2200/3500/5000) | PASS | Aggressive lag mitigation pass: reduced scene-model polling frequency and tightened projected-star budgets per FOV to lower CPU/GPU churn on active render path. |
| EV-0061 | module2-stars-full | G4 (partial) | `npm run typecheck && npm run test:module2` (snapshot updated via `npx vitest run tests/test_module2_deterministic_replay.test.js -u`) | `module2ParityFingerprint.ts` (`syncCadenceMs`, `starCap` samples), `tests/__snapshots__/test_module2_deterministic_replay.test.js.snap` | PASS | Extended deterministic fingerprint to cover active runtime perf knobs so Codex can detect drift in stabilization settings alongside existing stars parity samples. |
| EV-0062 | module0-foundation-lock / module1-hips-kernel / module2-stars-full | G6 regression sweep | `npm run typecheck && npm run test:module0 && npm run test:module1 && npm run test:module2` | `frontend/package.json` (`test:module0`), terminal outputs | PASS | Added dedicated `test:module0` script and validated module0/module1/module2 bundles together for cross-module regression confidence before Codex handoff. |
| EV-0063 | module2-stars-full | G2/G3 parity hardening | `npx vitest run tests/test_module2_deterministic_replay.test.js -u && npm run test:module2 && npm run test:module1 && npm run typecheck` | `runtimeFrame.ts` (`collectProjectedStars` removes forced `6.5` floor and finite projected-star cap), `test_module2_deterministic_replay.test.js.snap` | PASS | Removed local stabilization heuristics from star projection path to align closer with source-faithful `stars.c` limiting-magnitude flow; replay snapshot updated to reflect unbounded cap policy. |
| EV-0064 | module2-stars-full | G3/G4 (partial) | `npx vitest run tests/sky-engine-stars-runtime.test.js && npm run test:module2 && npm run test:module1 && npm run typecheck` | `StarsModule.ts` (`buildScenePacketSignature` + projection cache key now includes scene-packet slice), `tests/sky-engine-stars-runtime.test.js` | PASS | Stars runtime cache invalidation now keys on scene-packet content (count/id/mag + diagnostics slice) instead of object-count-only signature, preventing stale projection reuse when packet star payload mutates under stable object lists. |
| EV-0065 | module2-stars-full | G2/G3 parity hardening | `npm run test:module1 && npm run test:module2 && npm run typecheck` | `sceneAssembler.ts` (removed packet-time `limitingMagnitude` star prefilter and fallback label heuristics), `runtimeFrame.ts` + `StarsModule.ts` (selected-star-aware projection visibility path) | PASS | Continued direct stars runtime port by removing local packet assembly heuristics and moving star visibility decisions to render/runtime path; selected star now survives strict projection gating to maintain runtime selection continuity. |
| EV-0066 | module2-stars-full | G2 parity hardening | `npm run test:module1 && npm run test:module2 && npm run typecheck` | `SkyEngineScene.tsx` (`resolveSceneQueryLimitingMagnitude` removes `minimumByFov` floor), `runtimeFrame.ts` + `StarsModule.ts` (removed selected-star projection bypass) | PASS | Reverted non-source runtime heuristics and restored strict source-aligned limiting/visibility path so module2 behavior is not inflated by Hub-specific star floors or forced selected-star rendering. |
| EV-0069 | module2-stars-full | G3 runtime stabilization | `npm run test -- tests/test_hips_render_order.test.js tests/test_scene_query_state.test.js tests/test_file_backed_tile_repository_gaia_flow.test.js tests/test_tile_selection.test.js && npm run test:module2` | `hipsRenderOrder.ts` (`formatHipsViewportKey` quantizes viewport signature values) | PASS | Prevented reload thrash from sub-pixel `projectionMat11` jitter by quantizing `hipsViewport` signature/cache keys; runtime tile signatures now remain stable under tiny camera changes so scene packets can settle and star/object rendering can recover. |
| EV-0070 | module2-stars-full | G2/G3 (partial) | `npx vitest run tests/test_erfa_starpv.test.js && npx tsc --noEmit && npm run test:module2` | `erfaStarpv.ts` (ERFA `eraStarpv` + `eraEpb2jd`), `starsCatalogAstrom.ts` (Stellarium `stars.c::compute_pv` + `star_get_astrom`), `transforms/coordinates.ts` (`ObserverAstrometrySnapshot.ttJulianDate` + `earthPv`), `observerAstrometryMerge.ts` (wires derived geometry `ttJulianDate`/`earthPv`), `services/sceneAssembler.ts` (`resolveCatalogPv` + `projectStarFromPv` cache; `star_get_astrom` path when `plx > 2 mas`), `tests/test_erfa_starpv.test.js` (ERFA canonical golden vector) | PASS | Scene assembler now applies proper motion and parallax via the Stellarium source path (`compute_pv` → `star_get_astrom`) before handing the resulting astrometric ICRF unit vector to the observer-frame chain. Per-star `pv`-vectors are cached on the immutable `RuntimeStar` reference to match Stellarium's once-at-tile-load behaviour (`stars.c::on_file_tile_loaded`). Catalogue rows with `plx <= 2 mas` fall back to static ra/dec exactly as Stellarium's loader does. |
| EV-0073 | module0-foundation-lock / module1-hips-kernel / module2-stars-full | G7 handoff prep | `npm run typecheck && npm run build && npm run test:module0 && npm run test:module1 && npm run test:module2` | `docs/runtime/port/CODEX-HANDOFF.md` (new), `docs/runtime/port/README.md` (Canonical Files promotes `CODEX-HANDOFF.md`), `frontend/AGENTS.md` (§10 + §11 updated to direct fresh agents at the new handoff doc and reflect EV-0072 module 2 bundle state) | PASS | Handoff prep for Codex 5.3 / any successor taking over without prior chat context. New `CODEX-HANDOFF.md` is a single entry point (goal + hard constraints + pinned upstream commit + required-read order + per-module completion state + execution order + next coding targets + commands + evidence conventions + known residuals). `typecheck`, `build`, and all three module bundles (12/12, 43/43, 32/32 across 9 files) re-run green. Next free evidence ID is **EV-0074** (`EV-0067`/`EV-0068` remain intentionally unused). |
| EV-0072 | module2-stars-full | G3/G5 parity verification | `npx vitest run tests/test_module2_painter_project_frame_astrom.test.js && npm run typecheck && npm run test:module2` | `tests/test_module2_painter_project_frame_astrom.test.js` (new), `frontend/package.json` (`test:module2` adds `test_module2_painter_project_frame_astrom.test.js`), `.github/workflows/module2-stars.yml` (path filter adds the new test + `runtime/erfaAbLdsun.ts` + `runtime/observerDerivedGeometry.ts`) | PASS | Seals the `painter_project(FRAME_ASTROM → FRAME_OBSERVED)` claim end-to-end: confirms that stars with `parallaxMas > 2` are routed through `compute_pv` + `star_get_astrom` (EV-0070 pv path) and the resulting astrometric ICRF vector is taken through `eraLdsun` + `eraAb` + `bpn^T` + `ri2h` by `convertObserverFrameVector('icrf' → 'observed_geom')` — numerically matching a manual Stellarium-chain reference to 1e-11. Also locks the `plx ≤ 2 mas` fallback to `raDecToObserverUnitVector` (`stars.c::on_file_tile_loaded` dirty-plx rule). Module 2 bundle now at 32/32 across 9 files. |
| EV-0071 | module0-foundation-lock / module1-hips-kernel / module2-stars-full | G7 audit + G6 bundle expansion | `npm run typecheck && npm run build && npm run test:module0 && npm run test:module1 && npm run test:module2 && npm test` (residuals catalogued) | `frontend/package.json` (`test:module2` now includes `test_erfa_starpv.test.js`), `.github/workflows/module2-stars.yml` (path filter adds `erfaStarpv.ts`, `starsCatalogAstrom.ts`, `observerAstrometryMerge.ts`, `services/sceneAssembler.ts`, `transforms/coordinates.ts`, and `test_erfa_starpv.test.js`), `docs/runtime/port/module-inventory.md` (`src/hip.c`, `src/hip.h`, `src/modules/stars.c` row notes refreshed with actual Hub targets + evidence through EV-0070; Function-Level Expansion Queue bullet bumped to EV-0038–EV-0070; new "Module 2 Function Inventory (Stars Pipeline)" section with per-function status), `docs/runtime/port/module-gates.md` ("Known residuals" section catalogs 10 repo-wide `npm test` failures by owner module), `docs/runtime/port/README.md` (EV range EV-0038–EV-0070 + EV-0071), `docs/runtime/port/module2-source-contract.md` (§7 "Read first" + `test:module2` description + EV-0071 row), `docs/runtime/port/AUDIT-2026-04-20.md` (continuation audit pass B) | PASS | Full runtime-port doc audit reconciling claims with reality: module 0 and module 1 remain `COMPLETE` (narrow bundles PASS); module 2 remains `BLOCKED` with partials documented through EV-0070; module 2 gate bundle now exercises the EV-0070 ports; repo-wide `npm test` residuals (10 failures across 6 files) are explicitly catalogued in `module-gates.md` and attributed to module 3 / module 5 / module 6 / module 7 / module 8 owners (none are module 0/1/2 regressions). EV-0067 / EV-0068 remain intentionally unused IDs. |
| EV-0074 | module2-stars-full | G2/G3/G6 (partial) | `npm run typecheck && npm run test:module2 && npm run test:module1 && npm run test:module0` | `frontend/src/features/sky-engine/engine/sky/runtime/starsRenderVisitor.ts` (new `stars.c::render_visitor` traversal loop), `frontend/src/features/sky-engine/engine/sky/runtime/modules/runtimeFrame.ts` (consume visitor tile output + point-clipped gate), `frontend/src/features/sky-engine/engine/sky/runtime/modules/StarsModule.ts` (pass painter limits into projection path), `frontend/src/features/sky-engine/engine/sky/services/sceneAssembler.ts` + `contracts/scene.ts` (tile-ordered star packet payload), `frontend/tests/test_module2_stars_render_visitor.test.js` (new traversal regressions with pinned `stars.c` line citations), `frontend/package.json` (`test:module2` includes new test), `.github/workflows/module2-stars.yml` (paths updated for new source + tests) | PASS | Ports native `stars.c::render_visitor` tile traversal semantics into Hub runtime: parent→child visit flow, `limit_mag = fmin(stars_limit_mag, hard_limit_mag)`, per-tile `mag_min`/`mag_max` gates, in-tile break on first `vmag > limit_mag`, and point-clipped rejection path. `test:module2` now runs **35/35 across 10 files**; module1 and module0 sanity bundles remain **43/43** and **12/12**. |
| EV-0075 | module2-stars-full | G3/G6 (partial) | `npm run typecheck && npm run test:module2 && npm run test:module1 && npm run test:module0` | `frontend/src/features/sky-engine/engine/sky/adapters/starsLookup.ts` (`findRuntimeStarByHipInTiles` now uses a cached survey index keyed by `hip_get_pix(hip, 2)` and avoids linear all-tile scans), `frontend/tests/test_module2_stars_lookup_survey.test.js` (new survey traversal regressions with pinned `stars.c::obj_get_by_hip` line citations), `frontend/package.json` (`test:module2` includes `test_module2_stars_lookup_survey.test.js`), `.github/workflows/module2-stars.yml` (paths updated for new test) | PASS | Extends `obj_get_by_hip` from the earlier single-tile helper to survey-wide lookup semantics keyed by `hip_get_pix(hip, 2)` buckets on loaded non-Gaia survey tiles, mirroring Stellarium's bucketed tile search intent (`stars.c` lines 931-947). `test:module2` now runs **38/38 across 11 files**; module1 and module0 sanity bundles remain **43/43** and **12/12**. |
| EV-0076 | module2-stars-full | G4/G6 (partial) | `npm run typecheck && npm run build && npm run test:module2 && npm run test:module1 && npm run test:module0` | `frontend/src/features/sky-engine/engine/sky/runtime/module2ParityFingerprint.ts` (adds deterministic slices for native `render_visitor` traversal order/output, survey `obj_get_by_hip` lookup behavior, and ERFA/Stellarium catalog astrometry outputs), `frontend/tests/test_module2_deterministic_replay.test.js` (assertions for new fingerprint slices), `frontend/tests/__snapshots__/test_module2_deterministic_replay.test.js.snap` (updated golden snapshot) | PASS | Extends Module 2 deterministic replay coverage per `CODEX-HANDOFF.md` target #1: G4 fingerprint now locks EV-0074 traversal semantics, EV-0075 survey HIP lookup behavior, and EV-0070 catalog astrometry seam outputs in one stable snapshot. Module bundles remain green: **test:module2 39/39 (11 files)**, **test:module1 43/43**, **test:module0 12/12**. |
| EV-0077 | module2-stars-full | G2/G3/G6 (partial) | `npm run typecheck && npm run test:module1 && npm run test:module2` | `frontend/src/features/sky-engine/engine/sky/adapters/fileTileRepository.ts` (`compareSurveyByMaxVmag`, `normalizeSurveyOrderingAndGaiaGate`), `frontend/tests/test_file_backed_tile_repository_bounds.test.js` (new survey-order + Gaia-gate regressions) | PASS | Continues `stars_add_data_source` seam alignment from `stars.c`: survey ordering now treats `max_vmag = NaN` as +infinity (matching native `survey_cmp`), and Gaia `min_vmag` promotion now follows non-Gaia survey ceilings while preserving existing narrow-FOV Gaia activation override. Regression bundles remain green: **test:module1 45/45**, **test:module2 39/39**. |
| EV-0078 | module2-stars-full | G2/G3/G6 (partial) | `npm run typecheck && npm run test:module2 && npm run test:module1` | `frontend/src/features/sky-engine/engine/sky/adapters/starsList.ts` (new `listRuntimeStarsFromTiles`), `frontend/tests/test_module2_stars_list.test.js`, `frontend/src/features/sky-engine/engine/sky/contracts/tiles.ts` (`SkyTileProvenance.hipsOrder/hipsPix`), `frontend/package.json` (`test:module2` bundle), `.github/workflows/module2-stars.yml` (paths filter) | PASS | Lands a larger `stars.c::stars_list` parity seam over loaded runtime tiles: source-key selection, `max_mag` filtering, nuniq hint lookup via `nuniq_to_pix`, and `MODULE_AGAIN`-style status when hinted tile is unresolved. Module bundles remain green: **test:module2 42/42 (12 files)** and **test:module1 45/45**. |
| EV-0079 | module2-stars-full | G2/G3/G6 (partial) | `npm run typecheck && npm run test:module2 && npm run test:module1` | `frontend/src/features/sky-engine/engine/sky/adapters/starsList.ts` (source-key aware `stars_list` traversal), `frontend/src/features/sky-engine/engine/sky/contracts/tiles.ts` (`SkyTileProvenance.sourceKey/sourceKeys`), `frontend/src/features/sky-engine/engine/sky/adapters/fileTileRepository.ts` (merged tile provenance now carries survey source keys), `frontend/tests/test_module2_stars_list.test.js` (explicit source-key selection regression) | PASS | Expands `stars_list`/`stars_add_data_source` seam fidelity: list traversal can now select by source key (`stars_list(..., source)` intent) instead of only catalog buckets, while preserving nuniq hint behavior and `MODULE_AGAIN` unresolved-hint status. Module bundles remain green: **test:module2 43/43 (12 files)**, **test:module1 45/45**. |
| EV-0080 | module2-stars-full | G2/G3/G6 (partial) | `npm run typecheck && npm run test:module2 && npm run test:module1` | `frontend/src/features/sky-engine/engine/sky/contracts/tiles.ts` (`SkyTileProvenance.hipsTiles`), `frontend/src/features/sky-engine/engine/sky/adapters/fileTileRepository.ts` (Gaia tile provenance emits `hipsTiles`; merged provenance carries upstream order/pix set), `frontend/src/features/sky-engine/engine/sky/adapters/starsList.ts` (hint resolution matches `hipsTiles` set with source-key guard), `frontend/tests/test_module2_stars_list.test.js` (merged nuniq provenance regression) | PASS | Extends `stars_list(..., hint=nuniq)` parity on merged survey payloads: hint matching now works over a set of contributing HiPS `(order,pix)` entries instead of a single tile pair, preserving source-key scoping and `MODULE_AGAIN` fallback when unresolved. Module bundles remain green: **test:module2 44/44 (12 files)**, **test:module1 45/45**. |
| EV-0081 | module2-stars-full | G2/G3/G6 (partial) | `npm run typecheck && npm run test:module2 && npm run test:module1` | `frontend/src/features/sky-engine/engine/sky/adapters/starsList.ts` (source fallback + tile `mag_min` pruning + sorted per-tile traversal with max-mag early break), `frontend/tests/test_module2_stars_list.test.js` (unknown-source fallback regression + tile pruning/early-break regression) | PASS | Deepens `stars.c::stars_list` loop parity: unknown `source` now falls back to the first available survey key, non-hint traversal skips tiles where `mag_min >= max_mag`, and per-tile iteration breaks once sorted star magnitude exceeds `max_mag` (instead of scanning all rows). Module bundles remain green: **test:module2 46/46 (12 files)**, **test:module1 45/45**. |
| EV-0082 | module2-stars-full / Track A dev UX | G4/G6 (partial) + ops | `npm run typecheck && npm run test:module2 && npm run test:module1` | `frontend/src/features/sky-engine/engine/sky/runtime/module2ParityFingerprint.ts` (`starsListSlice` → `listRuntimeStarsFromTiles` frozen fixture), `frontend/tests/test_module2_deterministic_replay.test.js`, `frontend/tests/__snapshots__/test_module2_deterministic_replay.test.js.snap`, `scripts/docker-dev-backend.sh`, root `package.json` (`docker:dev-backend`) | PASS | **G4:** deterministic replay fingerprint now includes a `stars_list` loaded-tile slice (default traversal order + statuses, explicit `gaia` source traversal, hinted early-break path, unresolved-hint `again`) so drift in `starsList.ts` is caught by the golden snapshot alongside EV-0076 slices. **Track A:** one-command `npm run docker:dev-backend` starts `postgres`+`redis`+`backend` for local `cd frontend && npm run dev` against `127.0.0.1:8000`. Bundles: **test:module2 46/46 (12 files)**, **test:module1 45/45**. |
| EV-0083 | module2-stars-full | G3/G6 (partial) | `npm run typecheck && npm run build && npm run test:module2 && npm run test:module1` | `frontend/src/features/sky-engine/engine/sky/diagnostics/skyDiagnostics.ts` (`buildSkyDiagnostics` runs `listRuntimeStarsFromTiles` over visible tiles at `limitingMagnitude`), `frontend/src/features/sky-engine/engine/sky/contracts/scene.ts` (`diagnostics.starsListVisitCount`), `frontend/src/features/sky-engine/engine/sky/runtime/module2ParityFingerprint.ts` (synthetic packets carry the field), `frontend/tests/test_module2_stars_list.test.js` (live `assembleSkyScenePacket` regression), `frontend/tests/test_module2_stars_render_visitor.test.js`, `frontend/tests/sky-engine-stars-runtime.test.js`, `frontend/tests/test_sky_engine_scene_ownership.test.js`, `.github/workflows/module2-stars.yml` (path filter for `skyDiagnostics.ts`) | PASS | Wires the `stars_list` loaded-tile adapter into every assembled scene packet: diagnostics now expose `starsListVisitCount` (non-hint traversal at scene `limitingMagnitude` over `query.visibleTileIds`), so the port runs on the live assembly path—not only Vitest fixtures—while staying separate from deduped/projected star counts. Bundles: **test:module2 47/47 (12 files)**, **test:module1 45/45**. |
| EV-0084 | module2-stars-full / Track A UX | G3/G6 (partial) | `npm run typecheck && npm run build && npm run test:module2 && npm run test:module1` | `frontend/src/features/sky-engine/SkyEngineSnapshotStore.ts`, `frontend/src/features/sky-engine/engine/sky/runtime/modules/SnapshotBridgeModule.ts`, `frontend/src/pages/SkyEnginePage.tsx` (HUD **Listed** chip), `frontend/src/features/sky-engine/engine/sky/diagnostics/skyDiagnostics.ts` (`formatSkyDiagnosticsSummary`), `.github/workflows/module2-stars.yml` | PASS | Surfaces `diagnostics.starsListVisitCount` through the snapshot bridge into the Sky Engine HUD (**Listed** vs **Stars**) with an explanatory tooltip; summary string includes `list N` for log-style diagnostics. **test:module2** + **test:module1** green after change. |
| EV-0085 | module1-hips-kernel / Track A UX | G6 (partial) | `npm run typecheck && npm run build && npm run test:module1 && npm run test:module2` | `frontend/src/features/sky-engine/sceneQueryState.ts` (`resolveScenePacketForQuery` no longer promotes `tileLoadResult` on multi-survey when `resolvedTileQuerySignature` mismatches `tileQuerySignature`), `frontend/tests/test_scene_query_state.test.js` (regression), `frontend/src/styles.css` (inspector dock bottom-left + detail-shell pointer-events), `frontend/src/pages/SkyEnginePage.tsx` (**Tiles error** HUD chip when `diagnostics.sourceError`) | PASS | Fixes a real HUD inconsistency: mismatched signatures could assemble a **new** `visibleTileIds` query against **stale** `runtimeTiles`, producing **Listed 0** / empty star fields while planets still drew. Inspector moves to a **bottom-left** console closer to Stellarium’s info area (not a tall right rail). **test:module1 46/46**, **test:module2 47/47**. |
| EV-0086 | module2-stars-full | G3/G4/G6 (partial) | `npm run typecheck && npm run build && npm run test:module2 && npm run test:module1` | `luminanceReport.ts` (atmosphere aid off → skip bright sky sampling; daylight + atmosphere on → cap to **+5°** zenith reference), `solar.ts` (daylight star bands continuous at 8°), `module2ParityFingerprint.ts` (**`scene-lum|`** slice: atm-off 55°, daylight 72°/8°, night −18°), `test_scene_luminance_report.test.js` (includes **8°** fast-adaptation regression), `test_tone_adaptation.test.js`, `test_sky_engine_astronomy.test.js`, `test_module2_deterministic_replay.test.js` / snapshot, `package.json` / `module2-stars.yml` | PASS | **G4** replay fingerprint locks `evaluateSceneLuminanceReport` alongside stars seams; **8°** case aligns with `test_tone_adaptation` bright-step geometry. **test:module2 55/55 across 14 files**, **test:module1 46/46**. |
| EV-0087 | module2-stars-full | G3/G6 (partial) | `npm run typecheck && npm run build && npm run test:module2 && npm run test:module1` | `runtime/modules/runtimeFrame.ts` (`getObjectHorizonFade` uses **geometric** altitude via `observedToGeometricAltitudeDeg` only; no view-center mixing), `frontend/tests/test_runtime_frame_horizon.test.js`, `frontend/tests/sky-engine-runtime-frame-projection.test.js` (valid `starTiles` fixtures; expectations match `collectProjectedNonStarObjects` magnitude policy), `frontend/package.json` (`test:module2` adds projection suite), `.github/workflows/module2-stars.yml` (path filter) | PASS | Fixes sub-horizon planets/DSOs being treated as visible when the camera dipped slightly below the horizon. **`test:module2` 66/66 across 16 files**, **`test:module1` 46/46**. |
| EV-0088 | module2-stars-full | G4/G6 (partial) | `npm run test -- tests/test_module2_deterministic_replay.test.js -u && npm run typecheck && npm run build && npm run test:module2 && npm run test:module1` | `frontend/src/features/sky-engine/engine/sky/runtime/module2ParityFingerprint.ts` (adds `stars-projection` deterministic slice: fixed-input `collectProjectedStars` replay), `frontend/tests/test_module2_deterministic_replay.test.js` (asserts `stars-projection` marker), `frontend/tests/__snapshots__/test_module2_deterministic_replay.test.js.snap` (updated golden fingerprint), `docs/runtime/port/module2-source-contract.md`, `docs/runtime/port/module-gates.md`, `docs/runtime/port/module-inventory.md`, `docs/runtime/port/CODEX-HANDOFF.md`, `docs/runtime/port/README.md` | PASS | Extends G4 to lock active stars runtime projection output (from `runtimeFrame.ts::collectProjectedStars` on deterministic packet/view inputs) into the module2 fingerprint so drift in the `StarsModule` projection path is surfaced immediately. Validation remains green: **`test:module2` 66/66 (16 files)** and **`test:module1` 46/46**. |
| EV-0089 | module2-stars-full | G4/G6 (partial) | `npm run test -- tests/test_module2_deterministic_replay.test.js -u && npm run typecheck && npm run build && npm run test:module2 && npm run test:module1` | `frontend/src/features/sky-engine/engine/sky/runtime/module2ParityFingerprint.ts` (extends deterministic `stars-reuse|` slice with threshold edge/fail cases for projection-cache reuse), `frontend/tests/__snapshots__/test_module2_deterministic_replay.test.js.snap` (updated golden fingerprint), `docs/runtime/port/module-gates.md`, `docs/runtime/port/module-inventory.md` | PASS | Extends G4 frame-pacing coverage by freezing projection-cache reuse thresholds in deterministic replay, including edge-at-threshold (`edge`) and just-over-threshold (`fail`) cases, so pacing drift is detectable from the golden fingerprint without relying on unstable wall-clock timings. Validation remains green: **`test:module2` 66/66 (16 files)** and **`test:module1` 46/46**. |
| EV-0090 | module2-stars-full | G4/G6 (partial) | `npm run test -- tests/test_module2_stars_projection_reuse.test.js && npm run typecheck && npm run build && npm run test:module2 && npm run test:module1` | `frontend/tests/test_module2_stars_projection_reuse.test.js` (new reuse-threshold regression suite for `evaluateStarsProjectionReuse`), `frontend/package.json` (`test:module2` includes new suite), `.github/workflows/module2-stars.yml` (path filter includes new suite), `docs/runtime/port/module2-source-contract.md`, `docs/runtime/port/module-gates.md`, `docs/runtime/port/module-inventory.md`, `docs/runtime/port/CODEX-HANDOFF.md`, `docs/runtime/port/README.md` | PASS | Adds explicit module2 replay-adjacent regression coverage for StarsModule projection-cache reuse thresholds/streak behavior (including timestamp, FOV, and limiting-magnitude gates) as a standalone suite in the module2 gate bundle. Validation remains green: **`test:module2` 70/70 (17 files)** and **`test:module1` 46/46**. |
| EV-0091 | module2-stars-full | G3/G6 (partial) | `npm run test -- tests/test_module2_stars_lookup_survey.test.js tests/test_module2_stars_lookup.test.js && npm run typecheck && npm run build && npm run test:module2 && npm run test:module1` | `frontend/src/features/sky-engine/engine/sky/adapters/starsLookup.ts` (`findRuntimeStarByHipInTiles` now mirrors `stars.c::obj_get_by_hip` order traversal by probing `hip_get_pix(hip, 0)` then `hip_get_pix(hip, 1)` candidate buckets), `frontend/tests/test_module2_stars_lookup_survey.test.js` (adds explicit order-0-priority regression), `docs/runtime/port/module2-source-contract.md`, `docs/runtime/port/module-gates.md`, `docs/runtime/port/module-inventory.md`, `docs/runtime/port/CODEX-HANDOFF.md`, `docs/runtime/port/README.md` | PASS | Replaces the Hub-specific `hip_get_pix(hip, 2)` lookup bucket behavior with source-faithful `obj_get_by_hip` traversal ordering (`order 0` before `order 1`) while retaining non-Gaia filtering and stable first-match semantics on loaded runtime tiles. Validation now reports **`test:module2` 71/71 (17 files)** and **`test:module1` 46/46**. |
| EV-0092 | module2-stars-full | G2/G3/G6 (partial) | `npm run test -- tests/test_module2_stars_list.test.js tests/test_module2_deterministic_replay.test.js && npm run typecheck && npm run build && npm run test:module2 && npm run test:module1` | `frontend/src/features/sky-engine/engine/sky/adapters/starsList.ts` (non-hint traversal now keeps tile row order and uses `continue` when `star.mag > maxMag`, removing Hub sort+early-break heuristic), `frontend/tests/test_module2_stars_list.test.js` (regression updated to lock row-order/continue semantics), `docs/runtime/port/module2-source-contract.md`, `docs/runtime/port/module-gates.md`, `docs/runtime/port/module-inventory.md`, `docs/runtime/port/CODEX-HANDOFF.md`, `docs/runtime/port/README.md` | PASS | Aligns loaded-runtime `stars.c::stars_list` loop semantics with upstream: no local per-tile sort and no break on first over-limit star. This preserves source row traversal behavior and avoids dropping later valid stars in mixed-order tile payloads. Validation remains green: **`test:module2` 71/71 (17 files)** and **`test:module1` 46/46**. |
| EV-0093 | module2-stars-full | G2/G3/G6 (partial) | `npm run test -- tests/test_module2_stars_list.test.js tests/test_module2_deterministic_replay.test.js && npm run typecheck && npm run build && npm run test:module2 && npm run test:module1` | `frontend/src/features/sky-engine/engine/sky/adapters/starsList.ts` (hinted `stars_list` path now mirrors source by not applying `max_mag` filtering), `frontend/tests/test_module2_stars_list.test.js` (new hinted traversal regression locks no-`max_mag` behavior for `hint=nuniq`), `docs/runtime/port/module2-source-contract.md`, `docs/runtime/port/module-gates.md`, `docs/runtime/port/module-inventory.md`, `docs/runtime/port/CODEX-HANDOFF.md`, `docs/runtime/port/README.md` | PASS | Aligns `stars.c::stars_list` hint path with source behavior: once hinted tile resolves, traversal no longer filters rows by `max_mag` (upstream loops all tile rows, callback-controlled). Validation remains green: **`test:module2` 72/72 (17 files)** and **`test:module1` 46/46**. |
| EV-0094 | module2-stars-full | G2/G3/G6 (partial) | `npm run test -- tests/test_module2_stars_list_traversal.test.js tests/test_module2_stars_list.test.js tests/test_module2_deterministic_replay.test.js && npm run typecheck && npm run build && npm run test:module2 && npm run test:module1` | `frontend/src/features/sky-engine/engine/sky/adapters/starsListTraversal.ts` (new explicit `hips_iter`-style traversal cursor and no-hint visitor mirroring `stars.c::stars_list` loop + conditional `push_children` behavior), `frontend/src/features/sky-engine/engine/sky/adapters/starsList.ts` (non-hint path now runs through traversal module), `frontend/tests/test_module2_stars_list_traversal.test.js` (new traversal regression suite), `frontend/tests/test_module2_stars_list.test.js` (expects traversal-aligned order/behavior), `frontend/package.json` (`test:module2` includes new suite), `.github/workflows/module2-stars.yml` (path filters include new adapter+suite), `docs/runtime/port/module2-source-contract.md`, `docs/runtime/port/module-gates.md`, `docs/runtime/port/module-inventory.md`, `docs/runtime/port/CODEX-HANDOFF.md`, `docs/runtime/port/README.md` | PASS | Ports a larger `stars_list` seam: loaded-runtime traversal now uses an explicit iterator with `nextTile` + conditional child descent (children only visited when tile completes without callback interruption), matching `hips_iter_next`/`hips_iter_push_children` intent from `stars.c`. Validation remains green: **`test:module2` 79/79 (18 files)** and **`test:module1` 46/46**. |
| EV-0095 | module2-stars-full | G2/G3/G6 (partial) | `npm run test -- tests/test_module2_stars_survey_registry.test.js tests/test_file_backed_tile_repository_bounds.test.js && npm run typecheck && npm run build && npm run test:module2 && npm run test:module1` | `frontend/src/features/sky-engine/engine/sky/adapters/starsSurveyRegistry.ts` (new `stars_add_data_source` registration helpers: `survey_cmp` parity ordering, Gaia `min_vmag` promotion, activation-floor policy, Gaia entry `min_vmag`, active-survey load-plan), `frontend/src/features/sky-engine/engine/sky/adapters/fileTileRepository.ts` (repository load path now consumes shared survey-registry adapter), `frontend/tests/test_module2_stars_survey_registry.test.js` (new parity regression suite), `frontend/package.json` (`test:module2` includes new suite), `.github/workflows/module2-stars.yml` (path filters include new adapter+suite), `docs/runtime/port/module2-source-contract.md`, `docs/runtime/port/module-gates.md`, `docs/runtime/port/module-inventory.md`, `docs/runtime/port/CODEX-HANDOFF.md`, `docs/runtime/port/README.md` | PASS | Ports a larger `stars_add_data_source` seam on the active runtime path by extracting source-faithful survey ordering/gating logic from `fileTileRepository.ts` into a dedicated adapter and locking behavior with module2 tests. Validation remains green: **`test:module2` 96/96 (19 files)** and **`test:module1` 46/46**. |
| EV-0096 | module2-stars-full | G2/G3/G4/G6 (partial) | `npm run test -- tests/test_module2_stars_survey_registry.test.js tests/test_module2_deterministic_replay.test.js -u && npm run typecheck && npm run build && npm run test:module2 && npm run test:module1` | `frontend/src/features/sky-engine/engine/sky/adapters/starsSurveyRegistry.ts` (`compareStarsSurveyByMaxVmag` now mirrors `stars.c::survey_cmp` exactly with max-vmag-only compare), `frontend/tests/test_module2_stars_survey_registry.test.js` (tie-case regression now locks insertion-order behavior for equal `max_vmag`), `frontend/src/features/sky-engine/engine/sky/runtime/module2ParityFingerprint.ts` (new `survey-registry` deterministic slice), `frontend/tests/__snapshots__/test_module2_deterministic_replay.test.js.snap`, `docs/runtime/port/module2-source-contract.md`, `docs/runtime/port/module-gates.md`, `docs/runtime/port/module-inventory.md`, `docs/runtime/port/CODEX-HANDOFF.md`, `docs/runtime/port/README.md` | PASS | Tightens `stars_add_data_source` parity by removing Hub-only comparator tie-breakers (`min_vmag`/`key`) and freezing the resulting `survey_cmp` tie semantics in both module2 unit tests and G4 deterministic replay. Validation remains green: **`test:module2` 96/96 (19 files)** and **`test:module1` 46/46**. |
| EV-0097 | module2-stars-full | G4/G6 (partial) | `npm run test -- tests/test_module2_overlay_cadence.test.js tests/test_module2_deterministic_replay.test.js -u && npm run typecheck && npm run build && npm run test:module2 && npm run test:module1` | `frontend/src/features/sky-engine/engine/sky/runtime/overlayCadence.ts` (new pure `evaluateOverlayCadenceDecision` helper for overlay sync/view-change gating), `frontend/src/features/sky-engine/engine/sky/runtime/modules/OverlayRuntimeModule.ts` (runtime now consumes shared cadence decision helper), `frontend/src/features/sky-engine/engine/sky/runtime/module2ParityFingerprint.ts` (new `overlay-cadence` deterministic slice), `frontend/tests/test_module2_overlay_cadence.test.js` (new regression suite), `frontend/tests/__snapshots__/test_module2_deterministic_replay.test.js.snap`, `frontend/package.json` (`test:module2` includes cadence suite), `.github/workflows/module2-stars.yml` (path filters include cadence runtime + test), `docs/runtime/port/module2-source-contract.md`, `docs/runtime/port/module-gates.md`, `docs/runtime/port/module-inventory.md`, `docs/runtime/port/CODEX-HANDOFF.md`, `docs/runtime/port/README.md` | PASS | Extends G4 frame-pacing replay coverage by freezing overlay sync cadence decisions (steady no-op, wrap-around azimuth motion, guided-signature force sync, and initial sync) into deterministic snapshot + dedicated regression tests. Validation remains green: **`test:module2` 100/100 (20 files)** and **`test:module1` 46/46**. |
| EV-0098 | module2-stars-full | G2/G3/G6 (partial) | `npm run test -- tests/test_module2_stars_c_runtime_port.test.js tests/test_module2_stars_list.test.js tests/test_module2_stars_lookup.test.js tests/test_module2_stars_lookup_survey.test.js && npm run typecheck && npm run build && npm run test:module2 && npm run test:module1` | `frontend/src/features/sky-engine/engine/sky/adapters/starsCRuntimePort.ts` (new large `stars.c` runtime adapter for survey ordering, `stars_list` traversal semantics, `obj_get_by_hip` order probing, and loaded-tiles bridge), `frontend/tests/test_module2_stars_c_runtime_port.test.js` (new comprehensive regression suite), `frontend/src/features/sky-engine/engine/sky/adapters/starsLookup.ts` (lookup path now routed through the adapter's `findStarByHipFromPortSurveys` seam), `frontend/package.json` (`test:module2` includes `test_module2_stars_c_runtime_port.test.js`), `.github/workflows/module2-stars.yml` (path filters include `starsCRuntimePort.ts` and new suite), `docs/runtime/port/module2-source-contract.md`, `docs/runtime/port/module-gates.md`, `docs/runtime/port/module-inventory.md`, `docs/runtime/port/CODEX-HANDOFF.md`, `docs/runtime/port/README.md` | PASS | Lands a >500-line module2 seam that ports a larger `stars.c` runtime contract surface into a dedicated adapter with explicit list/lookup behavior, then validates it in both focused and full module bundles. Validation is green: **`test:module2` 119/119 (21 files)** and **`test:module1` 46/46**. |
| EV-0099 | module2-stars-full | G4/G6 (partial) | `npm run test -- tests/test_module2_deterministic_replay.test.js -u && npm run typecheck && npm run build && npm run test:module2 && npm run test:module1` | `frontend/src/features/sky-engine/engine/sky/runtime/module2ParityFingerprint.ts` (new deterministic `stars-c-runtime` slice that exercises `starsCRuntimePort.ts` survey ordering, source resolution, list semantics, and HIP lookup outcomes), `frontend/tests/test_module2_deterministic_replay.test.js` (asserts `stars-c-runtime|` marker), `frontend/tests/__snapshots__/test_module2_deterministic_replay.test.js.snap`, `docs/runtime/port/module2-source-contract.md`, `docs/runtime/port/module-gates.md`, `docs/runtime/port/module-inventory.md`, `docs/runtime/port/CODEX-HANDOFF.md`, `docs/runtime/port/README.md` | PASS | Extends G4 replay coverage to freeze the new large runtime adapter seam from EV-0098 in the module2 fingerprint snapshot. Validation remains green: **`test:module2` 119/119 (21 files)** and **`test:module1` 46/46**. |
| EV-0100 | module2-stars-full | G2/G3/G4/G6 (partial) | `npm run test -- tests/test_module2_stars_c_survey_lifecycle_port.test.js tests/test_module2_deterministic_replay.test.js -u && npm run typecheck && npm run build && npm run test:module2 && npm run test:module1` | `frontend/src/features/sky-engine/engine/sky/adapters/starsCSurveyLifecyclePort.ts` (new large source-faithful `stars.c` survey lifecycle seam: `on_file_tile_loaded`-style row normalization, `stars_add_data_source` properties/survey ordering/Gaia min-vmag promotion, `stars_list` hint/no-hint traversal, and `obj_get_by_hip` lookup over sync tile probes), `frontend/tests/test_module2_stars_c_survey_lifecycle_port.test.js` (new lifecycle parity regression suite), `frontend/src/features/sky-engine/engine/sky/runtime/module2ParityFingerprint.ts` (new deterministic `stars-c-survey-lifecycle` slice), `frontend/tests/test_module2_deterministic_replay.test.js`, `frontend/tests/__snapshots__/test_module2_deterministic_replay.test.js.snap`, `frontend/package.json` (`test:module2` includes lifecycle suite), `.github/workflows/module2-stars.yml` (path filters include new adapter+suite), `docs/runtime/port/module2-source-contract.md`, `docs/runtime/port/module-gates.md`, `docs/runtime/port/module-inventory.md`, `docs/runtime/port/CODEX-HANDOFF.md`, `docs/runtime/port/README.md` | PASS | Lands a >1000-line module2 seam that ports a wider `stars.c` lifecycle contract into a dedicated adapter and freezes it in G4 deterministic replay. Validation is green: **`test:module2` 141/141 (22 files)** and **`test:module1` 46/46**. |
| EV-0101 | module2-stars-full | G2/G3/G4/G6 (partial) | `npm run test -- tests/test_module2_stars_c_module_runtime_port.test.js tests/test_module2_deterministic_replay.test.js -u && npm run typecheck && npm run build && npm run test:module2 && npm run test:module1` | `frontend/src/features/sky-engine/engine/sky/adapters/starsCModuleRuntimePort.ts` (new large source-faithful `stars.c` module-runtime seam for datasource registration ordering, `stars_list` hint/no-hint traversal, `obj_get_by_hip` lookup order, `render_visitor` traversal surface, and designation lookup), `frontend/tests/test_module2_stars_c_module_runtime_port.test.js` (new module-runtime parity regression suite), `frontend/src/features/sky-engine/engine/sky/runtime/module2ParityFingerprint.ts` (new deterministic `stars-c-module-runtime` slice), `frontend/tests/test_module2_deterministic_replay.test.js`, `frontend/tests/__snapshots__/test_module2_deterministic_replay.test.js.snap`, `frontend/package.json` (`test:module2` includes module-runtime suite), `.github/workflows/module2-stars.yml` (path filters include new adapter+suite), `docs/runtime/port/module2-source-contract.md`, `docs/runtime/port/module-gates.md`, `docs/runtime/port/evidence-index.md`, `docs/runtime/port/CODEX-HANDOFF.md`, `docs/runtime/port/README.md` | PASS | Lands another >1000-line app-side module2 seam that ports `stars.c` module-runtime lifecycle contracts into a dedicated adapter and freezes outcomes in deterministic replay. Validation is green: **`test:module2` 150/150 (23 files)** and **`test:module1` 46/46**. |
| EV-0102 | module2-stars-full | G4/G6 (partial) | `npm run test:module2` | `frontend/src/features/sky-engine/engine/sky/adapters/framePacingDecisions.ts` (749-line frame-pacing decision seam for projection reuse, overlay cadence, runtime model sync, and deterministic trace digests), `frontend/tests/test_module2_frame_pacing_decisions.test.js` (222-line regression suite), `frontend/src/features/sky-engine/engine/sky/runtime/module2ParityFingerprint.ts` (`stars-c-frame-pacing` deterministic slice), `frontend/package.json` (`test:module2` includes frame-pacing suite), `.github/workflows/module2-stars.yml` (path filters include frame-pacing suite) | PASS | Reconciles the already-wired G4 frame-pacing trace seam against the module2 gate bundle. Validation is green: **`test:module2` 159/159 (24 files)**. Source reference: Stellarium `core.c::core_update` / `core_render` frame lifecycle and `utils/fps.c::fps_tick`; this is deterministic Hub-runtime replay coverage, not a G5 side-by-side parity closure. |
| EV-0103 | module2-stars-full | G2/G4/G6 (partial) | `npx vitest run tests/test_module2_stars_c_label_runtime_port.test.js` (RED before implementation: missing module), `npx vitest run tests/test_module2_stars_c_label_runtime_port.test.js`, `npx vitest run tests/test_module2_deterministic_replay.test.js -u`, `npm run typecheck`, `npm run build`, `npm run test:module0`, `npm run test:module1`, `npm run test:module2` | `frontend/src/features/sky-engine/engine/sky/adapters/starsCLabelRuntimePort.ts` (new large source-faithful `stars.c` label/name/designation seam for `parse_json_names`, `star_get_skycultural_name`, `name_is_bayer`, `star_get_bayer_name`, `star_render_name`, and `star_get_designations`), `frontend/tests/test_module2_stars_c_label_runtime_port.test.js` (new parity regression suite), `frontend/src/features/sky-engine/engine/sky/runtime/module2ParityFingerprint.ts` (`stars-c-label-runtime` deterministic slice), `frontend/tests/test_module2_deterministic_replay.test.js`, `frontend/tests/__snapshots__/test_module2_deterministic_replay.test.js.snap`, `frontend/package.json` (`test:module2` includes label runtime suite), `.github/workflows/module2-stars.yml` (path filters include new adapter+suite) | PASS | Lands a >500-line module2 code slice porting Stellarium `stars.c` label/name selection and designation behavior into a dedicated adapter. Validation is green: focused label suite **9/9**, deterministic replay **3/3**, `typecheck`, `build`, module0 **12/12**, module1 **46/46**, and **`test:module2` 168/168 (25 files)**. |
| EV-0104 | module2-stars-full | G5/G6 (partial) | `npm run typecheck && npm run test:module2` | `frontend/src/features/sky-engine/engine/sky/runtime/module2SideBySideParityHarness.ts` (new side-by-side parity harness fixture/checkpoint builder), `frontend/tests/test_module2_side_by_side_parity_harness.test.js` (new harness regression suite), `frontend/package.json` (`test:module2` includes side-by-side harness suite), `.github/workflows/module2-stars.yml` (path filters include harness source + suite) | PASS | Establishes a reproducible module2 side-by-side harness baseline against the pinned-source fixture set and wires it into CI/module2 bundle. Initial B-V fixture constants were corrected to the pinned fixture baseline before final validation. Validation is green: `typecheck` PASS and **`test:module2` 171/171 (26 files)**. G5 remains partial/open until checkpoints are sourced from live pinned Stellarium runtime outputs rather than committed fixture vectors. |
| EV-0105 | full-port audit | G7 (docs/audit) | `git status --short`; `git -C study/stellarium-web-engine/source/stellarium-web-engine-master rev-parse HEAD`; source C/H file count; `module-inventory.md` file-row count; file-level status count from `module-inventory.md`; `rg --files frontend/src/features/sky-engine` | `docs/runtime/port/AUDIT-2026-04-26.md`, `docs/runtime/port/README.md`, `docs/runtime/port/CODEX-HANDOFF.md`, `docs/runtime/port/module-gates.md`, `docs/runtime/port/module2-source-contract.md`, `docs/runtime/port/evidence-index.md` | PASS | Documentation-only full audit. Confirms local pinned source commit `63fb3279e85782158a6df63649f1c8a1837b7846`, 146 local C/H files, 146 inventory rows, only one file-level `PORTED` row, module2 still `BLOCKED`, and performance diagnosis should be "GPU draw exists, but Stellarium painter/render_gl/core batching and lifecycle are not ported." |
| EV-0106 | module2-stars-full | G5/G6/G7 (partial) | `npm run generate:module2-live-side-by-side-reference`; `npm run profile:sky-engine-runtime`; `npm run typecheck`; `npm run test -- tests/test_module2_stars_c_survey_lifecycle_port.test.js tests/test_module2_side_by_side_parity_harness.test.js`; `npm run test:module2`; `npm run build` | `frontend/scripts/generate_module2_live_side_by_side_reference.mjs` (live pinned Stellarium checkpoint capture), `frontend/src/features/sky-engine/engine/sky/runtime/module2SideBySideLiveReference.generated.ts` (captured live `stars_list` + `module_add_data_source` vectors), `frontend/src/features/sky-engine/engine/sky/runtime/module2SideBySideParityHarness.ts` (live vectors consumed in harness comparisons), `frontend/src/features/sky-engine/engine/sky/adapters/starsCSurveyLifecyclePort.ts` (live fetch lifecycle mapping through `propertiesHttpStatus`), `frontend/tests/test_module2_stars_c_survey_lifecycle_port.test.js`, `frontend/tests/test_module2_side_by_side_parity_harness.test.js`, `.cursor-artifacts/parity-compare/module2-live-runtime-profile-2026-04-26.json` (active-scene telemetry capture via `frontend/scripts/profile_sky_engine_runtime_perf.mjs`), `.github/workflows/module2-stars.yml` (path filters include live generator/generated file) | PASS | Closes the handoff-required live checkpoint ingestion step: side-by-side harness now consumes live pinned-runtime vectors and checks lifecycle branches (`again→ok`, `again→error`) against Hub adapter behavior. Active-scene telemetry artifact is now captured and tied to unresolved `core.c` / `painter.c` / `render_gl.c` parity gaps (CPU-side projection/model/buffer prep remains the tracked risk while GPU draw is present). Validation is green: focused suites **26/26**, `test:module2` **172/172 (26 files)**, `typecheck`, and `build` PASS. G5 remains open pending broader live checkpoint coverage. |
| EV-0107 | module0-foundation-lock / core-render-pivot | G3/G6/G7 (partial) | `npm run typecheck`; `npm run test -- tests/test_sky_core_frame_lifecycle_order.test.js`; `npm run build` | `study/stellarium-web-engine/source/stellarium-web-engine-master/src/core.c` (source extraction: `core_update` timing + update stack `285-347`; module-update sort/loop `338-344`; `core_render` pre-render stack `521-571`; render loop `572-574`; flush + post-render `586-598`), `frontend/src/features/sky-engine/engine/sky/runtime/SkyCore.ts` (new central `runFrameLifecycle` with per-frame update+render ownership and source-faithful phase order; removed dirty-frame render skip gate), `frontend/tests/test_sky_core_frame_lifecycle_order.test.js` (new minimal frame-order regression), `docs/runtime/port/core-c-frame-lifecycle-mapping.md` (Stellarium→SkyCore mapping table with exact source sections), `docs/runtime/port/module-inventory.md` (`src/core.c` row mapping update) | PASS | Render-pipeline pivot step 1 (core lifecycle only): SkyCore now executes one source-faithful frame controller each engine tick: `updateServices` → core-update preamble → ordered module updates → core-render preamble → ordered module renders → Babylon scene render → ordered post-render. Timing model now mirrors `core_update` minimum-dt clamp behavior (`>= 0.001s`) and removes React/dirty-frame render gating from lifecycle ownership. Painter/render_gl internals are intentionally untouched in this step. |
| EV-0108 | module0-foundation-lock / core-render-pivot | G3/G6/G7 (partial) | `npm run typecheck`; `npm run test -- tests/test_sky_core_frame_lifecycle_order.test.js`; `npm run build` | `study/stellarium-web-engine/source/stellarium-web-engine-master/src/core.c` (`core_render` preamble state setup `537-570`), `frontend/src/features/sky-engine/engine/sky/runtime/types.ts` (new `SkyCoreFrameState`/`SkyCoreRenderState` plus lifecycle context wiring), `frontend/src/features/sky-engine/engine/sky/runtime/SkyCore.ts` (per-frame render-state creation and single-object pass-through across ordered `update`/`render`/`postRender` phases), `frontend/tests/test_sky_core_frame_lifecycle_order.test.js` (asserts one shared `frameState` object per frame across preamble/update/render/postRender), `docs/runtime/port/core-c-frame-lifecycle-mapping.md` (new `core_render` preamble value mapping table) | PASS | Render-pipeline pivot step 2: SkyCore now materializes an explicit frame/render state object mirroring `core_render` pre-object setup (window, pixel scale, framebuffer size, painter mag limits) and threads it through the ordered module lifecycle while keeping `scene.render()` in place. No `painter.c` or `render_gl.c` implementation was introduced. |
| EV-0109 | module0-foundation-lock / painter-api-surface | G3/G6/G7 (partial) | `npm run typecheck`; `npm run test -- tests/test_sky_core_frame_lifecycle_order.test.js`; `npm run build` | `study/stellarium-web-engine/source/stellarium-web-engine-master/src/painter.h` (public enums/struct/callable API surface), `study/stellarium-web-engine/source/stellarium-web-engine-master/src/painter.c` (`paint_prepare`/`paint_finish` frame lifecycle seam), `frontend/src/features/sky-engine/engine/sky/runtime/renderer/painterPort.ts` (new no-op painter boundary with source-named API methods and per-frame queue lifecycle), `frontend/src/features/sky-engine/engine/sky/runtime/types.ts` (`SkyCoreRenderState.render.painter`), `frontend/src/features/sky-engine/engine/sky/runtime/SkyCore.ts` (core render preamble now resets painter per frame and runs `paint_prepare` before ordered module render), `frontend/tests/test_sky_core_frame_lifecycle_order.test.js` (new assertions: painter reference threaded into preamble/render and per-frame queue reset), `docs/runtime/port/painter-c-api-surface-mapping.md` (API inventory/mapping) | PASS | Painter API surface slice 1 only: a source-shaped painter boundary exists and is threaded through frame render state with per-frame preamble reset, while batching/shader/render_gl behavior remains intentionally unported. |
| EV-0110 | module0-foundation-lock / painter-lifecycle-correction | G3/G6/G7 (partial) | `npm run typecheck`; `npm run test -- tests/test_sky_core_frame_lifecycle_order.test.js`; `npm run build` | `study/stellarium-web-engine/source/stellarium-web-engine-master/src/core.c` (`obj_render` loop `572-574`, `paint_finish` `586-587`, `post_render` loop `595-598`), `study/stellarium-web-engine/source/stellarium-web-engine-master/src/painter.c` (`paint_finish` seam), `frontend/src/features/sky-engine/engine/sky/runtime/SkyCore.ts` (calls `frameState.render.painter.paint_finish()` after ordered module render and before `scene.render()`), `frontend/tests/test_sky_core_frame_lifecycle_order.test.js` (asserts `painter.paint_finish` order: after module render and before scene render; verifies frame queue contains `paint_finish`), `docs/runtime/port/core-c-frame-lifecycle-mapping.md`, `docs/runtime/port/painter-c-api-surface-mapping.md`, `docs/runtime/port/module-inventory.md` | PASS | Correction slice only: paint-finish seam is now wired into frame lifecycle order without batching/render_gl/shader implementation; `scene.render` remains after `paint_finish`, and `postRender` remains after `scene.render`. |

## Rules

- Every `PASS` gate must reference at least one evidence row.
- Evidence rows must be reproducible with committed command definitions.
