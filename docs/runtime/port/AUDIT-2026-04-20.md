# Runtime Port Audit — 2026-04-20

This file captures the Codex handoff audit (**EV-0056**), the follow-up full audit (**EV-0071**), and the Codex 5.3 handoff prep (**EV-0073**). Each pass reconciles `docs/runtime/port/` with the code in `frontend/src/` and the test/CI bundles.

---

## Pass A — 2026-04-20 (EV-0056): Codex handoff

### Scope audited
- `README.md`
- `module-gates.md`
- `module2-source-contract.md`
- `evidence-index.md`
- `stellarium-web-engine-src.md`
- `module-inventory.md`
- `blockers.md`
- `module0-source-contract.md`
- `module1-source-contract.md`
- `module0-eraApco-port-plan.md`
- `module0-eraEpv00-port-plan.md`

### Outcomes
1. Canon updated to enforce **exact parity target** (logic + UI behavior), not approximation.
2. Docs updated to reflect **self-contained Hub runtime/tooling** for active port work (no dependency on external local source trees).
3. Module 2 evidence drift fixed:
   - Historical dock/night staging rows retained but marked superseded where appropriate.
   - Active parity state clarified for Codex handoff.
4. Source-root wording normalized to pinned upstream `src/` reference across contracts/inventory.
5. Handoff constraints for Codex explicitly documented in `module2-source-contract.md`.

### Hard constraints for Codex (still current)
- Continue exact source-faithful porting only.
- Keep runtime assets and active tooling inside Astronomy Hub.
- Do not show `Stellarium` as user-facing Sky-Engine branding text.
- Run verification per pass: `npm run typecheck`, `npm run test:module2`, plus lints on touched files.

---

## Pass B — 2026-04-20 (EV-0071): Full audit post-EV-0070

Driven by a user request to re-audit the codebase and update the runtime docs (especially `module-inventory.md` and `module-gates.md`) because the module status narratives were suspected of drifting from reality.

### What was verified
- `npm run typecheck` — PASS.
- `npm run build` — PASS.
- `npm run test:module0` — PASS (12/12).
- `npm run test:module1` — PASS (43/43).
- `npm run test:module2` — PASS (29/29 across 8 files) **after** expanding the bundle to include `tests/test_erfa_starpv.test.js` (previously present as a file but not in the gate bundle, so the EV-0070 port had no automated gate signal).
- `npm test` (repo-wide) — FAIL: 10 assertions across 6 files (catalogued in `module-gates.md` "Known residuals"; none live in the module 0/1/2 gate bundles).

### Drift corrected
1. **Gate signal drift:** `frontend/package.json` `test:module2` did not cover the newest EV-0070 port. Bundle and the `.github/workflows/module2-stars.yml` path filter now include `test_erfa_starpv.test.js`, `erfaStarpv.ts`, `starsCatalogAstrom.ts`, `observerAstrometryMerge.ts`, `services/sceneAssembler.ts`, and `transforms/coordinates.ts` so the gate tracks the code that was wired up.
2. **Stale evidence range citations:**
   - `README.md` refreshed from "EV-0038–EV-0056" to "EV-0038–EV-0070" (+ **EV-0071**).
   - `module2-source-contract.md §7` "Read first" line updated to point at "EV-0038–EV-0071" and reference the new "Known residuals" section in `module-gates.md`.
3. **Inventory narrative drift:**
   - `src/hip.c`, `src/hip.h`, and `src/modules/stars.c` rows in `module-inventory.md` now list the actual Hub TS targets and evidence IDs (through EV-0070) instead of pointing only at the contract file.
   - The Function-Level Expansion Queue bullet for `module2-stars-full` now enumerates the landed partial-parity surface (EV-0038–EV-0070) rather than only "bv_to_rgb Hub port (EV-0038)".
4. **Missing module 2 function inventory:** `module-inventory.md` previously carried a Module 0 function inventory but none for module 2, so the partial ports of `hip_get_pix`, `nuniq_to_pix`, `render_visitor` `limit_mag`, `compute_pv`, and `star_get_astrom` were not discoverable by walking the inventory alone. A new "Module 2 Function Inventory (Stars Pipeline)" section now lists status (`PORTED` / `BLOCKED`) per Stellarium function against concrete Hub paths, evidence IDs, and tests.
5. **Repo-wide residuals undocumented:** the 10 `npm test` failures did not appear anywhere in the port docs. They are now explicitly catalogued in `module-gates.md` "Known residuals", split by target module so future owners pick them up:
   - `sky-engine-runtime-frame-projection.test.js` (3) — belongs to module 3 / module 5 / module 7 non-star visibility gate.
   - `test_close_fov_star_counts.test.js` (1) — asset-heavy probe, intentionally excluded from `test:module1` per `module1-source-contract.md` §3.
   - `test_sky_engine_overlay_persistence.test.js` (3), `test_sky_engine_scene_ownership.test.js` (1), `test_sky_engine_shell_decoupling.test.js` (1), `test_skyculture_runtime_switching.test.js` (1) — Sky-Engine shell / UI drift that belongs to module 6 / module 8.
6. **`module2-source-contract.md` handoff commands** — "test:module2" description expanded to list the actual files in the bundle (no longer just "BV, nuniq, limit mag, HIP"); evidence table extended with EV-0071.

### Gate-status claims after audit
| Module | Claim | Verified |
|---|---|---|
| `module0-foundation-lock` | COMPLETE | ✅ bundle PASS; no regressions in module 0 scope. |
| `module1-hips-kernel` | COMPLETE | ✅ bundle PASS (43/43). `test_close_fov_star_counts.test.js` remains an intentionally excluded asset-heavy probe per contract §3. |
| `module2-stars-full` | BLOCKED (partial G2/G3/G4 landed) | ✅ partials are each evidence-backed; module bundle PASS (29/29). G5–G7 remain FAIL. |
| `module3`–`module7` | N/A | ✅ per execution-order rule; no false-positive claims found. |

### Evidence linkage
- Pass A: `evidence-index.md` row **EV-0056**.
- Pass B: `evidence-index.md` row **EV-0071**.

---

## Pass C — 2026-04-20 (EV-0073): Codex 5.3 handoff prep

Driven by an explicit user request to prepare the port project for Codex 5.3 to resume without prior chat context.

### What was produced
- **New:** `docs/runtime/port/CODEX-HANDOFF.md` — single entry point covering goal + hard constraints + pinned upstream commit + required-read order + per-module completion state (module 0 COMPLETE 12/12, module 1 COMPLETE 43/43, module 2 BLOCKED/partial 32/32 across 9 files) + execution order + next coding targets (native `render_visitor` tile traversal, survey-wide `obj_get_by_hip`, G4 deterministic-coverage extension) + commands + CI workflow references + evidence conventions (next free ID: **EV-0074**) + known user-visible symptoms with doc pointers.
- **Updated:** `docs/runtime/port/README.md` Canonical Files list promotes `CODEX-HANDOFF.md` to position 1 and calls out the "Next free ID" counter.
- **Updated:** `frontend/AGENTS.md` §10 / §11 redirect fresh agents at `CODEX-HANDOFF.md` and carry the post-EV-0072 module 2 bundle state (32/32 across 9 files) + extended verification guidance (also update CI `paths:` when adding new sources/tests).

### What was verified
- `npm run typecheck` — PASS.
- `npm run build` — PASS (size warning pre-existing and out of scope).
- `npm run test:module0` — PASS (12/12).
- `npm run test:module1` — PASS (43/43).
- `npm run test:module2` — PASS (32/32 across 9 files, matching the EV-0072 gate).

### Gate-status claims after Pass C
Unchanged from Pass B. The handoff prep is a docs-only pass; no gate promotions and no demotions.

### Evidence linkage
- Pass C: `evidence-index.md` row **EV-0073**.
