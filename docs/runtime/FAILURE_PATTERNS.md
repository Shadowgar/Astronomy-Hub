## USAGE CLARIFICATION

This document supports debugging and system improvement.

It must never be used to:

* define system behavior
* override architecture or validation rules
* introduce new execution steps

# FAILURE PATTERNS — ASTRONOMY HUB (SUPPORT MEMORY, NON-AUTHORITATIVE)

## 0. PURPOSE
Record reusable failure archetypes and prevention checks for future sessions.

## 1. NON-AUTHORITY RULE (CRITICAL)
This file is non-authoritative support memory.
It must never override system rules, validation law, context law, or phase authority.
It must never be treated as a required core authority document.

## 2. SCOPE LIMITS
This file is:
- not a bug log
- not a changelog
- not a planning notebook
- not speculative notes

This file stores only recurring failure patterns and reusable prevention rules.

## 3. ENTRY TEMPLATE (REQUIRED)
Use this exact structure for each recorded pattern:

- ID:
- Date:
- Area:
- Failure type:
- Trigger:
- Root cause:
- Fix applied:
- Prevention rule:
- Required future check:
- Related docs or files:

## 4. USAGE RULE
Consult this file only in debug/review/reconciliation/planning contexts.
Do not use this file to authorize execution actions.

## 5. INITIAL STATE
No patterns recorded yet.
Add entries only after validated remediation outcomes.
